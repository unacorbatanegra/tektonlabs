import 'package:occam/occam.dart';

class MaintenanceController extends StateController {
  void onTap() {
    // navigator.pop();
    // 500.milliseconds.delay(() => Get.find<Rootstate.().onRefresh());
    // Get.until((route) => route.settings.name == '/');
  }
}
